import React from "react";
import { Text, StyleSheet, View } from "react-native";

const SignInPageScreen = () => {
  //arrow function and function() is just the same

  return (
    <View>
      <Text>Savorite</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default SignInPageScreen;
